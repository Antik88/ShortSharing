import { styled } from '@mui/system';
import { Button } from '@mui/material';

export const StyledButton = styled(Button)(
  {
    color: "primary",
    border: '3px solid',
    borderRadius: '0',
    variant: "outlined",
  });