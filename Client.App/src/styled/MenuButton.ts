import { styled } from '@mui/system';
import { Button } from '@mui/material';

export const MenuButton = styled(Button)(
    {
        color: "primary.light",
        textTransform: 'none',
        width: '100%',
        textAlign: 'left'
    });